import { youtubeUrlHelper } from 'viedor/helpers/youtube-url-helper';
import { module, test } from 'qunit';

module('Unit | Helper | youtube url helper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = youtubeUrlHelper([42]);
  assert.ok(result);
});
