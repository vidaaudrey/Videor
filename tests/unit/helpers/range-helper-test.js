import { rangeHelper } from 'viedor/helpers/range-helper';
import { module, test } from 'qunit';

module('Unit | Helper | range helper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = rangeHelper([42]);
  assert.ok(result);
});
