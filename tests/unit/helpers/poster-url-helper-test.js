import { posterUrlHelper } from 'viedor/helpers/poster-url-helper';
import { module, test } from 'qunit';

module('Unit | Helper | poster url helper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = posterUrlHelper([42]);
  assert.ok(result);
});
